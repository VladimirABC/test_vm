# -*- mode: ruby -*-
# vim: set ft=ruby :

Vagrant.configure("2") do |config|

	config.vm.box = "centos/7"
	config.vm.box_version = "20221105.1"
        config.vm.host_name = "centos"
        config.vm.provider "virtualbox" do |vb|
		vb.memory = "1024"
		vb.cpus = "2"
        end
end

